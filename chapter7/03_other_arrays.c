#include <stdio.h>

int main()
{
    //! LetsCodez: Scripting The Future
    int arr[5] = {1, 2, 3, 4, 5}; //? Declaration of an array with values
    for (int i = 0; i < 5; i++)
    {
        printf("The value of element %d of the array is %d\n", i, arr[i]);
    }
    return 0;
}