#include <stdio.h>

int main()
{
    //! LetsCodez: Scripting The Future!
    int a = 10; // A variable name can start with an alphabet

    int A = 12; // case sensetive both a and A are different

    int _b = 20; // A variable name can start with an underscore

    int d$ = 40; // A variable name cannot start with a special character

    int e_f = 50; // A variable name can contain an underscore

    // int 1c = 30; // A variable name cannot start with a number

    // int g h = 60; // A variable name cannot contain a space
    return 0;
}