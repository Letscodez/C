#include <stdio.h>

int main()
{
    //! LetsCodez: Scripting The Future!
    int a = 4, b = 3;
    int c = a + b;
    // a and b are operands and + is operator
    printf("The value of a is %d \nAnd value of b is %d\nAnd value go their sum is %d\n", a, b, c);
    int d = a % b;
    // % is modulus operator
    printf("When a is divided by b remainder is %d\n", d);

    return 0;
}