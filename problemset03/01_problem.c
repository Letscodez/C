#include <stdio.h>

int main()
{
    //! LetsCodez: Scripting The Future!
    // What will be the output of the following code?
    int a = 10;
    if (a = 11)
        printf("I am 11");
    else
        printf("I am not 11");
    //? a = 11 is an assignment operator, it will assign 11 to a and the condition will be true
    //? so the output will be "I am 11"
    //! correct answer: I am 11
    return 0;
}