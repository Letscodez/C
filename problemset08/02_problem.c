#include <stdio.h>

int main() 
{
    //! LetsCodez: Scripting The Future
    //? 3. Write your own version of strlen function from <string.h>
    char str[10] = "LetsCodez";
    printf("Length of string is: %d\n", len(str));
    return 0;
}