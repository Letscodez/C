#include <stdio.h>

int main() 
{
    //! LetsCodez: Scripting The Future
    //? Gets function
    char name[20];
    printf("Enter your name: ");
    gets(name); //? gets() is used to get a string input from the user
    puts(name); //? puts() is used to print a string
    return 0;
}