#include <stdio.h>

int main() 
{
    //! LetsCodez: Scripting The Future
    //? Strcmp function
    char str1[] = "Hello";
    char str2[] = "Hello";
    printf("%d \n", strcmp(str1, str2)); //* strcmp() function is used to compare two strings. It returns 0 if the strings are equal, a positive value if the first string is greater than the second string, and a negative value if the first string is less than the second string.
    return 0;
}